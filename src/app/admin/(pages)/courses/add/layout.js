
export async function generateMetadata() {
  return {
    title: "Admin Panel - Add Courses",
  };
}

export default function CourseAddLayout({ children }) {
  return (
    <>
      {children}
    </>
  );
}
